import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class SettingsInfrastructure {
  // Cette infrastructure est maintenant vide car les statistiques sont gérées par StatisticsService
  // Conservée pour compatibilité avec le store si nécessaire
}

