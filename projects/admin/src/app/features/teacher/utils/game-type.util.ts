/**
 * Utilitaires pour la normalisation et la comparaison des noms de types de jeux
 * Re-export depuis @shared/utils pour compatibilité avec l'admin
 * Utilise les données de la DB comme source de vérité
 */

// Re-export depuis @shared pour centraliser les constantes et utilitaires
export * from '@shared/utils/game-type.util';
